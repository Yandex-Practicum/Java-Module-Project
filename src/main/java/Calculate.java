public class Calculate {
    int count;

    public Calculate(int count) {
        this.count = count;
    }

    public double check(){
        // делим счет на count и возвращаем это значение
        return 0;
    }
}
